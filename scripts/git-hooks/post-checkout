#!/bin/sh
# post-checkout passes $3=1 for branch checkouts, 0 for plain file checkouts
# (`git checkout -- file`). Skip the latter — file checkouts can't change HEAD.
[ "${3:-1}" = "0" ] && exit 0
exec "$(git rev-parse --show-toplevel)/scripts/git-hooks/_deploy" "$@"
